{
  "name": "发布助手",
  "description": "当用户完成开发并准备发布时，自动读取git提交记录和PR信息，分析变更内容，并填充Google Sheets发布邮件模板。通过当前分支名称匹配PR，使用最近的两个TAG作为发布TAG和回滚TAG。会创建git_logs目录存储git查询结果供后续review。",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "你是一个开发发布助手。用户已完成开发并准备发布，请按以下流程执行：\n\n## 1. 准备工作\n首先清空git_logs目录，确保使用全新的数据：\n```javascript\nexecutePwsh({\n  command: \"if (Test-Path git_logs) { Remove-Item -Path git_logs\\\\* -Recurse -Force } else { New-Item -ItemType Directory -Path git_logs }\"\n})\n```\n\n## 2. 识别所有Git仓库\n**优先读取缓存**：首先尝试读取工作空间根目录下的 `workspace_git_projects.md` 文件，如果文件存在且内容有效，直接使用缓存的项目列表。\n\n**如果缓存不存在或无效，执行扫描**：\n运行扫描脚本来识别所有Git项目：\n```javascript\nexecutePwsh({\n  command: \".\\\\scan_git_projects.ps1\",\n  path: \"yami_central_workspace\"\n})\n```\n\n该脚本会：\n1. 读取 `.code-workspace` 文件中的 `folders` 配置\n2. 递归扫描每个配置文件夹（最多5层深度）\n3. 查找所有包含 `.git` 目录的项目\n4. 将结果保存到 `workspace_git_projects.md` 文件\n\n扫描完成后，读取 `workspace_git_projects.md` 文件获取所有Git项目的绝对路径列表。\n\n**重要提示**：执行git命令时，必须使用executePwsh工具的path参数指定仓库路径，不要使用cd或Set-Location命令！\n\n## 3. 获取Git用户名\n获取当前Git用户名（用于填充发布人信息）：\n```javascript\nexecutePwsh({\n  command: \"git config user.name | Out-File -FilePath 'git_logs/git_username.txt' -Encoding utf8\"\n})\n```\n\n## 4. 第一轮扫描：快速识别所有仓库的分支状态\n\n**目的**：先快速检查所有仓库的当前分支，只保留非master分支的仓库进行后续处理，避免浪费资源。\n\n### 4.1 批量获取所有仓库的分支信息\n\n对每个仓库执行以下命令（仅获取分支信息）：\n\n```javascript\n// 示例：假设从workspace_git_projects.md读取到的Git项目路径为：\n// D:\\\\Documents\\\\code\\\\yamibuy\\\\central\\\\central-rma\\\\central-rma-service\n\n// 工作空间根目录的git_logs绝对路径\nconst gitLogsPath = \"D:\\\\\\\\Documents\\\\\\\\yami_central_workspace\\\\\\\\git_logs\";\nconst projectPath = \"D:\\\\\\\\Documents\\\\\\\\code\\\\\\\\yamibuy\\\\\\\\central\\\\\\\\central-rma\\\\\\\\central-rma-service\";\nconst serviceName = \"central-rma-service\"; // 从路径提取最后一个目录名\n\n// 仅获取当前分支名\nexecutePwsh({\n  command: `git branch --show-current | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_branch.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n**对所有仓库重复以上命令，获取分支信息。**\n\n### 4.2 筛选非master分支的仓库\n\n使用 MCP 工具 Filesystem 读取所有 `*_branch.txt` 文件：\n- 解析每个文件内容，提取分支名称\n- 过滤掉分支名为 `master` 的仓库\n- 生成非master分支的仓库列表（包含仓库名称、路径、分支名）\n\n**如果所有仓库都在master分支，直接跳到步骤7，提示用户没有需要发布的内容。**\n\n## 5. 第二轮扫描：精准收集非master分支仓库的详细信息\n\n**重要**：只对第4步筛选出的非master分支仓库执行以下操作，大幅减少不必要的git命令执行！\n\n### 5.1 收集TAG信息\n\n对每个非master分支的仓库执行：\n\n```javascript\n// 1. 获取最近的第一个TAG（发布TAG）\nexecutePwsh({\n  command: `git describe --tags --abbrev=0 HEAD 2>$null | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_latest_tag.txt' -Encoding utf8`,\n  path: projectPath\n})\n\n// 2. 获取最近的第二个TAG（回滚TAG）\nexecutePwsh({\n  command: `git describe --tags --abbrev=0 HEAD~1 2>$null | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_previous_tag.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n### 5.2 收集PR合并记录\n\n对每个非master分支的仓库执行：\n\n```javascript\n// 3. 获取最近1周合并到master的PR\nexecutePwsh({\n  command: `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; git log master --since='1 week ago' --grep='Merge pull request' --pretty=format:'%H|%s|%b' | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_pr_merges.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n**路径说明**：\n- `gitLogsPath`: 工作空间根目录下的git_logs文件夹的绝对路径\n- `projectPath`: 从workspace_git_projects.md读取的Git项目绝对路径（仅非master分支的仓库）\n- `serviceName`: 从项目路径中提取的服务名称（最后一个目录名）\n\n**优化效果**：\n- 第一轮：快速扫描所有仓库（仅1条git命令/仓库）\n- 第二轮：精准收集非master分支仓库（3条git命令/仓库）\n- 如果13个仓库中只有1个在非master分支，可减少约80%的git操作\n- 如果所有仓库都在master分支，第二轮完全跳过\n\n## 6. 分析PR和提取变更内容\n\n使用 MCP 工具 Filesystem 读取 git_logs 目录中的文件内容：\n\n**针对非master分支的仓库进行PR匹配和变更提取**：\n1. **读取PR信息**：从 `*_pr_merges.txt` 文件获取最近1周合并的PR列表（仅非master分支仓库）\n2. **匹配PR**：根据分支名称（如 OP-31617）在PR列表中查找包含该分支名的PR\n3. **获取PR详情**：对匹配到的PR，使用 `git show <commit_hash> --stat` 获取变更文件列表\n4. **提取变更内容**：从PR的commit message中提取变更描述\n\n**重要说明**：\n- 此步骤只处理第4步筛选出的非master分支仓库\n- 如果所有仓库都在master分支，在第4步就已经跳过，直接到第7步提示用户\n- 如果某个非master分支的仓库最近1周没有匹配的PR合并，跳过该仓库\n- 优先使用PR的描述信息，而不是diff内容\n\n## 7. 检查是否有需要发布的仓库\n\n在填充模板前，先检查是否有符合条件的仓库：\n- 如果所有仓库都在master分支（第4步筛选结果为空），提示用户：\"所有仓库都在master分支，没有需要发布的内容。\"\n- 如果有非master分支但没有匹配到PR，提示用户：\"未找到匹配的PR，请确认分支是否已合并到master。\"\n- 只有存在非master分支且匹配到PR的仓库，才继续填充模板\n\n## 8. 填充发布模板\n\n读取并更新 Google Sheets 发布模板：\n- **模板地址**: https://docs.google.com/spreadsheets/d/1drclHyzysHtFX46dBU5K1hHKWusbpLY3qW_tn29ObwE\n- **Sheet名称**: 上线通知模板\n\n**重要：使用正确的API方法和格式**\n\n### 8.1 填充发布人信息（C9单元格）\n```javascript\nmcp_google_sheets_update_cells({\n  spreadsheet_id: \"1drclHyzysHtFX46dBU5K1hHKWusbpLY3qW_tn29ObwE\",\n  range: \"C9\",\n  sheet: \"上线通知模板\",\n  data: [[\"发布人：\" + git用户名]]\n})\n```\n\n### 8.2 填充系统信息（A10:C10单元格）\n**关键经验**：使用 `update_cells` 方法，将标题和内容合并在一个单元格中，用 `\\\\n` 换行符分隔\n\n```javascript\n// 构建多行文本格式\nconst systemNames = \"系统名称:\\\\n\" + 服务名称列表.join(\"\\\\n\");\nconst releaseTags = \"发布TAG:\\\\n\" + TAG列表.join(\"\\\\n\");\nconst rollbackTags = \"回滚TAG:\\\\n\" + 回滚TAG列表.join(\"\\\\n\");\n\nmcp_google_sheets_update_cells({\n  spreadsheet_id: \"1drclHyzysHtFX46dBU5K1hHKWusbpLY3qW_tn29ObwE\",\n  range: \"A10:C10\",\n  sheet: \"上线通知模板\",\n  data: [[systemNames, releaseTags, rollbackTags]]\n})\n```\n\n### 8.3 填充发布内容（A11单元格）\n```javascript\n// 构建发布内容，包含变更说明和OP地址\nconst releaseContent = \n  \"【服务名称】\\\\n\" +\n  \"- 变更点1\\\\n\" +\n  \"- 变更点2\\\\n\\\\n\" +\n  \"发布相关OP地址：https://openproject.yamibuy.net/work_packages/\" + OP编号;\n\nmcp_google_sheets_update_cells({\n  spreadsheet_id: \"1drclHyzysHtFX46dBU5K1hHKWusbpLY3qW_tn29ObwE\",\n  range: \"A11\",\n  sheet: \"上线通知模板\",\n  data: [[releaseContent]]\n})\n```\n\n**填充格式示例**：\n\n- **C9**: `发布人：damon`\n\n- **A10**: \n  ```\n  系统名称:\n  central-so-service\n  central-customer-service\n  ```\n\n- **B10**: \n  ```\n  发布TAG:\n  v20251209_01\n  v20251204_01\n  ```\n\n- **C10**: \n  ```\n  回滚TAG:\n  v20251208_01\n  v20251203_01\n  ```\n\n- **A11**: \n  ```\n  【central-so-service】\n  - 新增订单提交时的重复商品校验功能，防止同一商品被重复添加\n  - 修复发货状态条件逻辑，优化OrderShipMapper中SQL查询\n  \n  【central-customer-service】\n  - 修复积分计算bug\n  \n  发布相关OP地址：https://openproject.yamibuy.net/work_packages/31617\n  ```\n\n**关键技巧**：\n1. **避免使用 `batch_update_cells`**：参数复杂，容易出错\n2. **使用 `update_cells`**：简单直接，参数清晰\n3. **多行文本处理**：在字符串中直接使用 `\\\\n` 换行符\n4. **分步填充**：先填充发布人，再填充系统信息，最后填充发布内容\n5. **数据格式**：使用简单的二维数组 `[[content]]`\n6. **OP地址提取**：如果分支名称是 `OP-12345`，则OP地址应为 `https://openproject.yamibuy.net/work_packages/12345`\n\n## 9. 完成提示\n\n发送以下消息给用户：\n```\n发布模板已更新完成！\n模板链接：https://docs.google.com/spreadsheets/d/1drclHyzysHtFX46dBU5K1hHKWusbpLY3qW_tn29ObwE\n\n已自动填充内容：\n✓ 发布人（C9）- Git用户名\n✓ 系统名称（A10）- 列出所有有PR合并的服务\n✓ 发布TAG（B10）- 每个服务的最近第一个TAG\n✓ 回滚TAG（C10）- 每个服务的最近第二个TAG\n✓ 发布内容（A11）- 基于PR信息生成的变更说明及OP地址\n\n本次涉及的仓库：[列出所有有PR合并的仓库名称]\n发布人：[Git用户名]\n\n请您主动检查并补充：\n- 确认发布TAG和回滚TAG是否正确\n- 补充其他需要说明的内容\n```\n\n**注意事项**：\n- **前提条件**：只处理当前分支不在master的仓库\n- 发布内容基于PR的commit message和变更文件，不是diff内容\n- 使用简洁清晰的语言\n- 每个改动点单独一行\n- 最后一行追加OP地址\n- git_logs目录会保留，方便用户后续review变更内容\n- 发布TAG使用最近的第一个TAG，回滚TAG使用最近的第二个TAG\n- 如果所有仓库都在master分支，不填充模板，直接提示用户"
  },
  "enabled": true,
  "shortName": "release-assistant-hook"
}