{
  "name": "测试发布文档助手",
  "description": "对比当前工作空间下多个git项目的变更（当前分支 vs master），将所有输出写入git_logs目录，生成面向测试人员的发布文档，重点突出测试要点、配置变更和服务分支信息",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "请执行以下任务来生成测试发布文档：\n\n## 任务目标\n对比当前工作空间下所有git项目的变更（当前分支对比master分支），生成一份面向测试人员的发布文档。\n\n## 执行步骤\n\n### 1. 准备工作\n首先清空git_logs目录，确保使用全新的数据：\n```javascript\nexecutePwsh({\n  command: \"if (Test-Path git_logs) { Remove-Item -Path git_logs\\\\* -Recurse -Force } else { New-Item -ItemType Directory -Path git_logs }\"\n})\n```\n\n### 2. 识别所有Git仓库\n**优先读取缓存**：首先尝试读取工作空间根目录下的 `workspace_git_projects.md` 文件，如果文件存在且内容有效，直接使用缓存的项目列表。\n\n**如果缓存不存在或无效，执行扫描**：\n运行扫描脚本来识别所有Git项目：\n```javascript\nexecutePwsh({\n  command: \".\\\\scan_git_projects.ps1\",\n  path: \"yami_central_workspace\"\n})\n```\n\n该脚本会：\n1. 读取 `.code-workspace` 文件中的 `folders` 配置\n2. 递归扫描每个配置文件夹（最多5层深度）\n3. 查找所有包含 `.git` 目录的项目\n4. 将结果保存到 `workspace_git_projects.md` 文件\n\n扫描完成后，读取 `workspace_git_projects.md` 文件获取所有Git项目的绝对路径列表。\n\n### 3. 第一轮扫描：快速识别所有仓库的分支状态\n\n**目的**：先快速检查所有仓库的当前分支，只保留非master分支的仓库进行后续处理，避免浪费资源。\n\n#### 3.1 批量获取所有仓库的分支信息\n\n对每个仓库执行以下命令（仅获取分支信息）：\n\n```javascript\n// 示例：假设从workspace_git_projects.md读取到的Git项目路径为：\n// D:\\\\Documents\\\\code\\\\yamibuy\\\\central\\\\central-rma\\\\central-rma-service\n\n// 工作空间根目录的git_logs绝对路径\nconst gitLogsPath = \"D:\\\\\\\\Documents\\\\\\\\yami_central_workspace\\\\\\\\git_logs\";\nconst projectPath = \"D:\\\\\\\\Documents\\\\\\\\code\\\\\\\\yamibuy\\\\\\\\central\\\\\\\\central-rma\\\\\\\\central-rma-service\";\nconst serviceName = \"central-rma-service\"; // 从路径提取最后一个目录名\n\n// 仅获取当前分支名\nexecutePwsh({\n  command: `git branch --show-current | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_branch.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n**对所有仓库重复以上命令，获取分支信息。**\n\n#### 3.2 筛选非master分支的仓库\n\n使用 MCP 工具 Filesystem 读取所有 `*_branch.txt` 文件：\n- 解析每个文件内容，提取分支名称\n- 过滤掉分支名为 `master` 的仓库\n- 生成非master分支的仓库列表（包含仓库名称、路径、分支名）\n\n**如果所有仓库都在master分支，直接跳到步骤6，提示用户没有需要生成文档的内容。**\n\n### 4. 合并master到当前分支（仅非master分支）\n\n**重要**：只对第3步筛选出的非master分支仓库执行合并操作！\n\n**在收集变更信息之前，先执行合并操作确保代码是最新的**：\n\n运行合并脚本：\n```javascript\nexecutePwsh({\n  command: \".\\\\merge_master_to_current.ps1\",\n  path: \"工作空间根目录路径\"\n})\n```\n\n该脚本会：\n1. 遍历所有Git项目\n2. 检查当前分支（跳过已在master的仓库）\n3. 检查未提交的更改（有未提交更改会跳过并提示）\n4. 拉取最新的master代码（git fetch origin master）\n5. 合并master到当前分支（git merge origin/master）\n6. 如果遇到冲突，自动中止合并并记录冲突信息\n\n**合并结果处理**：\n- ✅ 如果所有仓库合并成功或已是最新，继续执行后续步骤\n- ⚠️ 如果有冲突，暂停流程并提示用户：\n  - 列出有冲突的仓库和分支\n  - 提供冲突解决步骤指南\n  - 建议用户手动解决冲突后重新运行hook\n- ❌ 如果有失败，记录失败原因并继续处理其他仓库\n\n**注意事项**：\n- 合并操作是安全的，遇到冲突会自动中止\n- 有未提交更改的仓库会被跳过，不会丢失代码\n- 合并成功后，当前分支会包含最新的master代码\n- **优化效果**：由于第3步已筛选，只对非master分支仓库执行合并，节省时间\n\n**重要**：只对第3步筛选出的非master分支仓库执行以下操作，大幅减少不必要的git命令执行！\n\n#### 5.1 收集提交记录和差异信息\n\n对每个非master分支的仓库执行以下操作，**所有输出必须写入git_logs目录**：\n\n**重要提示**：执行git命令时，必须使用executePwsh工具的path参数指定仓库路径，不要使用cd或Set-Location命令！\n\n**重要**：必须使用绝对路径来指定输出文件位置！\n\n```javascript\n// 示例：假设从第3步筛选出的非master分支Git项目路径为：\n// D:\\\\Documents\\\\code\\\\yamibuy\\\\central\\\\central-activity\\\\central-activity-service\n\n// 工作空间根目录的git_logs绝对路径\nconst gitLogsPath = \"D:\\\\\\\\Documents\\\\\\\\yami_central_workspace\\\\\\\\git_logs\";\nconst projectPath = \"D:\\\\\\\\Documents\\\\\\\\code\\\\\\\\yamibuy\\\\\\\\central\\\\\\\\central-activity\\\\\\\\central-activity-service\";\nconst serviceName = \"central-activity-service\"; // 从项目路径中提取的服务名称（最后一个目录名）\n\n// 1. 获取提交记录（当前分支相对于master的新提交）\nexecutePwsh({\n  command: `git log master..HEAD --oneline --no-merges | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_commits.txt' -Encoding utf8`,\n  path: projectPath\n})\n\n// 2. 获取变更统计信息\nexecutePwsh({\n  command: `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; git diff master --stat | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_diff_stat.txt' -Encoding utf8`,\n  path: projectPath\n})\n\n// 3. 获取变更文件列表\nexecutePwsh({\n  command: `git diff master --name-only | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_diff_files.txt' -Encoding utf8`,\n  path: projectPath\n})\n\n// 4. 获取完整差异内容\nexecutePwsh({\n  command: `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; git diff master | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_full_diff.txt' -Encoding utf8`,\n  path: projectPath\n})\n\n// 5. 获取远程仓库名称（用于生成PR链接）\nexecutePwsh({\n  command: `git remote get-url origin | Split-Path -Leaf | ForEach-Object { $_ -replace '\\.git$', '' } | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_remote_name.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n**路径说明**：\n- `gitLogsPath`: 工作空间根目录下的git_logs文件夹的绝对路径（固定值）\n- `projectPath`: 从第3步筛选出的非master分支Git项目绝对路径\n- `serviceName`: 从项目路径中提取的服务名称（最后一个目录名）\n- 所有输出文件必须使用绝对路径，避免相对路径计算错误\n\n**优化效果**：\n- 第一轮：快速扫描所有仓库（仅1条git命令/仓库）\n- 第二轮：精准收集非master分支仓库（5条git命令/仓库）\n- 如果13个仓库中只有2个在非master分支，可减少约75%的git操作\n- 如果所有仓库都在master分支，第二轮完全跳过\n\n对每个非master分支的仓库重复以上命令，替换相应的路径和服务名称。\n\n基于收集的信息，重点分析：\n- 接口变更（Controller层）- 需要接口测试\n- 数据库变更（Mapper/SQL）- 需要数据验证\n- 配置变更（Apollo/CMS Config）- 需要配置检查\n- Redis Key变更 - 需要缓存验证\n- 定时任务变更 - 需要任务执行验证\n- MQ消息变更 - 需要消息流转验证\n- 跨服务调用关系 - 需要联调测试\n\n**重要说明**：\n- 此步骤只处理第3步筛选出的非master分支仓库\n- 如果所有仓库都在master分支，在第3步就已经跳过，直接到第7步提示用户\n- 如果某个非master分支的仓库没有变更，在文档中标注\"无变更\"\n\n### 7. 检查是否有需要生成文档的仓库\n\n在生成文档前，先检查是否有符合条件的仓库：\n- 如果所有仓库都在master分支（第3步筛选结果为空），提示用户：\"所有仓库都在master分支，没有需要生成测试文档的内容。\"\n- 如果有非master分支但没有变更，提示用户：\"非master分支仓库没有变更，无需生成测试文档。\"\n- 只有存在非master分支且有变更的仓库，才继续生成文档\n\n### 8. 生成测试发布文档\n按照以下模板生成markdown文档，保存到工作空间根目录， 并且遵循以下要求：\n1. 如果当前服务分支是master，则不需要在服务分支信息罗列出来\n2. 远程仓库名称获取方式`git remote get-url origin | Split-Path -Leaf | ForEach-Object { $_ -replace '\\.git$', '' }`\n\n```markdown\n# [分支号] [功能标题]\n\n## 📋 基本信息\n- **生成时间**: [YYYY-MM-DD HH:MM:SS]\n- **涉及服务**: [N个]\n- **主要分支**: [主分支号]\n\n## 🔧 服务分支信息\n| 服务名称 | 当前分支 | 变更文件数 |\n|---------|---------|----------|\n| [远程仓库名称1] | [[分支名]](https://github.com/yamibuy/[远程仓库名称]/pulls?q=is:pr+head:[分支名]) | X |\n| [远程仓库名称2] | [[分支名]](https://github.com/yamibuy/[远程仓库名称]/pulls?q=is:pr+head:[分支名]) | X |\n\n---\n\n## 🎯 测试重点\n\n### [远程仓库名称1]\n**功能概述**: [一句话说明本次变更的核心功能]\n\n**测试要点**:\n- [ ] **接口测试**: `[HTTP方法] [接口路径]` - [功能说明]\n- [ ] **定时任务**: `[任务类名]` - [执行逻辑]\n- [ ] **MQ消息**: `[队列名/类名]` - [消息处理逻辑]\n- [ ] **数据库**: [涉及的表和操作类型]\n\n---\n\n### [远程仓库名称2]\n**功能概述**: [一句话说明本次变更的核心功能]\n\n**测试要点**:\n- [ ] **接口测试**: `[HTTP方法] [接口路径]` - [功能说明]\n- [ ] **定时任务**: `[任务类名]` - [执行逻辑]\n- [ ] **MQ消息**: `[队列名/类名]` - [消息处理逻辑]\n- [ ] **数据库**: [涉及的表和操作类型]\n\n---\n\n## ⚙️ 配置变更\n\n### [远程仓库名称1]\n\n#### Apollo配置\n```properties\n# 配置说明：[说明配置的用途和影响]\nconfig.key1=value1\nconfig.key2=value2\n```\n\n#### CMS/Config配置\n```properties\n# 配置说明：[说明配置的用途和影响]\nconfig.key1=value1\nconfig.key2=value2\n```\n\n#### SQL变更\n```sql\n-- 说明：[说明SQL变更的目的]\nALTER TABLE table_name ADD COLUMN column_name VARCHAR(255);\nCREATE INDEX idx_name ON table_name(column_name);\n```\n\n#### Redis Key变更\n```\n# Key说明：[说明Key的用途和影响范围]\nredis:key:pattern:*\nredis:another:key\n```\n\n---\n\n### [远程仓库名称2]\n\n#### Apollo配置\n```properties\n# 配置说明：[说明配置的用途和影响]\nconfig.key1=value1\n```\n\n#### CMS/Config配置\n```properties\n# 配置说明：[说明配置的用途和影响]\nconfig.key1=value1\n```\n\n#### SQL变更\n```sql\n-- 说明：[说明SQL变更的目的]\nCREATE TABLE new_table (\n  id BIGINT PRIMARY KEY,\n  name VARCHAR(255)\n);\n```\n\n#### Redis Key变更\n```\n# Key说明：[说明Key的用途和影响范围]\nredis:key:pattern:*\n```\n\n---\n\n## 🔗 服务依赖关系\n[如果涉及跨服务调用，用简单的流程说明]\n\n示例：\n```\n远程A → 服务B → 服务C\n```\n\n---\n\n## ⚠️ 测试注意事项\n1. **环境准备**: [需要的配置、数据准备等]\n2. **测试顺序**: [如果有依赖关系，说明测试先后顺序]\n3. **回归测试**: [需要回归的功能点]\n4. **数据验证**: [需要验证的数据库表或缓存]\n```\n\n### 9. 文档保存\n- 文件名格式：`[分支号] [功能标题].md`（例如：`feature-123 优化订单查询性能.md`）\n- 如果多个服务使用不同分支，使用主要分支号或用下划线连接多个分支号\n- 保存位置：项目根目录\n\n## 注意事项\n1. **所有git命令输出必须写入git_logs目录**，避免shell交互输出过多\n2. **两轮扫描优化**：\n   - 第一轮：快速获取所有仓库分支信息，筛选非master分支\n   - 第二轮：仅对非master分支仓库收集详细变更信息\n   - 如果所有仓库都在master分支，第二轮完全跳过\n3. 如果某个服务当前分支就是master，在第一轮就被过滤掉\n4. 如果某个非master分支服务没有变更，在文档中标注\"无变更\"\n4. **服务分支信息表格中的分支名必须生成为超链接**：\n   - 格式：`[分支名](https://github.com/yamibuy/服务名/pulls?q=is:pr+head:分支名)`\n   - 例如：`[feature-123](https://github.com/yamibuy/ec-so-service/pulls?q=is:pr+head:feature-123)`\n   - 这样可以直接点击跳转到对应的PR列表\n5. **重点关注测试相关内容**：\n   - Controller层的接口变更 → 接口测试清单\n   - Mapper层的SQL变更 → 数据验证清单\n   - application.yml或Apollo配置的变更 → 配置检查清单\n   - ConfigService/LangConfigService相关配置 → 配置验证\n   - 跨服务调用的影响 → 联调测试场景\n   - 定时任务和MQ消息 → 异步流程验证\n6. **不要包含开发细节**：\n   - 不要列出每次commit的详细信息\n   - 不要列出所有修改的文件清单\n   - 不要描述代码实现细节\n   - 只关注测试人员需要知道的内容\n7. 使用中文生成所有内容\n8. **git_logs目录会保留**，方便用户后续review变更内容"
  },
  "enabled": true,
  "shortName": "multi-repo-release-doc"
}