{
  "name": "切换master并更新代码",
  "description": "在会话开始时，自动将工作区中的所有Git仓库切换到master分支，并从远程拉取最新代码",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "请执行以下任务来切换所有仓库到master分支并更新代码：\n\n## 任务目标\n调用 `switch_to_master.ps1` 脚本，将当前工作空间下所有git项目切换到master分支，并从远程拉取最新代码。\n\n## 执行步骤\n\n### 1. 执行切换脚本\n运行 `switch_to_master.ps1` 脚本来自动处理所有仓库：\n\n```javascript\nexecutePwsh({\n  command: \".\\\\switch_to_master.ps1\",\n  path: \"yami_workspace\"\n})\n```\n\n该脚本会自动：\n1. 读取 `workspace_git_projects.md` 文件获取所有Git项目列表\n2. 对每个仓库执行：\n   - 检查是否有未提交的修改\n   - 切换到master分支\n   - 从远程拉取最新代码\n3. 输出执行结果统计\n\n### 2. 分析执行结果\n脚本执行完成后，分析输出内容：\n\n**成功情况**：\n- 如果所有仓库都成功切换并更新，简单告知用户操作完成\n- 列出有代码更新的仓库（如果有）\n\n**失败情况**：\n脚本会输出失败的仓库及原因，常见失败原因包括：\n\n1. **Uncommitted changes（未提交的修改）**：\n   - 识别：输出中包含 \"Uncommitted changes\"\n   - 处理：告知用户该仓库有未提交的修改，需要用户手动处理（提交或暂存）\n   - 提示：列出具体的仓库名称，建议用户使用 `git status` 查看详情\n\n2. **Checkout failed（切换分支失败）**：\n   - 识别：输出中包含 \"Checkout failed\"\n   - 处理：可能是分支不存在或有冲突，告知用户需要手动检查\n   - 提示：建议用户在该仓库目录下手动执行 `git checkout master`\n\n3. **Pull failed（拉取代码失败）**：\n   - 识别：输出中包含 \"Pull failed\"\n   - 处理：可能是网络问题或有冲突，告知用户需要手动处理\n   - 提示：建议用户检查网络连接或手动执行 `git pull`\n\n4. **Path not exists（路径不存在）**：\n   - 识别：输出中包含 \"Path not exists\"\n   - 处理：缓存的项目路径已失效，建议重新扫描\n   - 提示：建议用户运行 `scan_git_projects.ps1` 重新扫描\n\n### 3. 输出结果\n根据执行情况，向用户输出：\n\n**全部成功时**：\n```\n✓ 所有仓库已成功切换到master分支并更新\n总计：X个仓库\n有更新：Y个仓库（如果有，列出仓库名称）\n```\n\n**部分失败时**：\n```\n⚠ 部分仓库切换失败，需要手动处理\n\n成功：X个仓库\n失败：Y个仓库\n\n失败的仓库：\n- [仓库名]: [失败原因] - [处理建议]\n- [仓库名]: [失败原因] - [处理建议]\n\n请手动处理失败的仓库后，可以再次运行此Hook。\n```\n\n### 4. 无法自主处理的情况\n如果遇到以下情况，向用户请求帮助：\n\n1. `workspace_git_projects.md` 文件不存在\n   - 提示：请先运行 `scan_git_projects.ps1` 扫描Git项目\n\n2. 脚本执行出现异常错误（非预期的错误）\n   - 提示：脚本执行异常，请检查错误信息并手动处理\n\n3. 大量仓库失败（超过50%）\n   - 提示：大量仓库切换失败，可能存在系统性问题，建议检查网络连接或Git配置\n\n## 注意事项\n1. 使用中文输出所有信息\n2. 对于失败的仓库，提供清晰的处理建议\n3. 不要尝试自动修复需要用户决策的问题（如未提交的修改）\n4. 保持输出简洁，只在有问题时详细说明"
  },
  "enabled": true,
  "shortName": "checkout-master-pull"
}