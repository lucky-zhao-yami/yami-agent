{
  "name": "单元测试生成助手",
  "description": "基于当前分支与master分支的代码差异，自动识别变更的Java文件并生成对应的Spock单元测试。支持自动定位测试文件、创建测试类、覆盖核心逻辑分支和边界条件",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "你是一个单元测试生成助手。请按照以下流程为当前分支的代码变更生成单元测试。\n\n## 执行流程\n\n### 1. 准备工作\n首先清空git_logs目录，确保使用全新的数据：\n```javascript\nexecutePwsh({\n  command: \"if (Test-Path git_logs) { Remove-Item -Path git_logs\\\\* -Recurse -Force } else { New-Item -ItemType Directory -Path git_logs }\"\n})\n```\n\n### 2. 识别所有Git仓库\n**优先读取缓存**：首先尝试读取工作空间根目录下的 `workspace_git_projects.md` 文件，如果文件存在且内容有效，直接使用缓存的项目列表。\n\n**如果缓存不存在或无效，执行扫描**：\n运行扫描脚本来识别所有Git项目：\n```javascript\nexecutePwsh({\n  command: \".\\\\scan_git_projects.ps1\",\n  path: \"yami_central_workspace\"\n})\n```\n\n该脚本会：\n1. 读取 `.code-workspace` 文件中的 `folders` 配置\n2. 递归扫描每个配置文件夹（最多5层深度）\n3. 查找所有包含 `.git` 目录的项目\n4. 将结果保存到 `workspace_git_projects.md` 文件\n\n扫描完成后，读取 `workspace_git_projects.md` 文件获取所有Git项目的绝对路径列表。\n\n**重要提示**：执行git命令时，必须使用executePwsh工具的path参数指定仓库路径，不要使用cd或Set-Location命令！\n\n### 3. 第一轮扫描：快速识别所有仓库的分支状态\n\n**目的**：先快速检查所有仓库的当前分支，只保留非master分支的仓库进行后续处理，避免浪费资源。\n\n#### 3.1 批量获取所有仓库的分支信息\n\n对每个仓库执行以下命令（仅获取分支信息）：\n\n```javascript\n// 示例：假设从workspace_git_projects.md读取到的Git项目路径为：\n// D:\\\\Documents\\\\code\\\\yamibuy\\\\central\\\\central-rma\\\\central-rma-service\n\n// 工作空间根目录的git_logs绝对路径\nconst gitLogsPath = \"D:\\\\\\\\Documents\\\\\\\\yami_central_workspace\\\\\\\\git_logs\";\nconst projectPath = \"D:\\\\\\\\Documents\\\\\\\\code\\\\\\\\yamibuy\\\\\\\\central\\\\\\\\central-rma\\\\\\\\central-rma-service\";\nconst serviceName = \"central-rma-service\"; // 从路径提取最后一个目录名\n\n// 仅获取当前分支名\nexecutePwsh({\n  command: `git branch --show-current | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_branch.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n**对所有仓库重复以上命令，获取分支信息。**\n\n#### 3.2 筛选非master分支的仓库\n\n使用 MCP 工具 Filesystem 读取所有 `*_branch.txt` 文件：\n- 解析每个文件内容，提取分支名称\n- 过滤掉分支名为 `master` 的仓库\n- 生成非master分支的仓库列表（包含仓库名称、路径、分支名）\n\n**如果所有仓库都在master分支，直接跳到步骤8，提示用户没有需要生成测试的内容。**\n\n### 4. 第二轮扫描：精准收集非master分支仓库的代码变更\n\n**重要**：只对第3步筛选出的非master分支仓库执行以下操作，大幅减少不必要的git命令执行！\n\n#### 4.1 获取变更的Java文件\n\n对每个非master分支的仓库执行以下命令，获取变更的Java文件：\n\n**重要**：必须使用绝对路径来指定输出文件位置！\n\n```javascript\n// 仅针对非master分支的仓库执行以下命令\n\n// 获取变更的Java文件列表\nexecutePwsh({\n  command: `git diff master..HEAD --name-only --diff-filter=AM | Select-String '\\\\.java$' | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_changed_files.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n**优化效果**：\n- 第一轮：快速扫描所有仓库（仅1条git命令/仓库）\n- 第二轮：精准收集非master分支仓库（1条git命令/仓库）\n- 如果13个仓库中只有2个在非master分支，可减少约75%的git操作\n- 如果所有仓库都在master分支，第二轮完全跳过\n\n对每个非master分支的仓库重复以上命令，替换相应的路径和服务名称。\n\n**仅对非master分支仓库中变更的Java文件**，获取具体的代码差异：\n\n**重要**：必须使用绝对路径来指定输出文件位置！\n\n```javascript\n// 对于每个变更的文件（仅非master分支仓库）\nconst fileName = \"UserService\"; // 从文件路径中提取的类名\nconst filePath = \"src/main/java/com/yamibuy/service/UserService.java\";\n\nexecutePwsh({\n  command: `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; git diff master..HEAD -U10 -- ${filePath} | Out-File -FilePath '${gitLogsPath}\\\\\\\\${serviceName}_${fileName}_diff.txt' -Encoding utf8`,\n  path: projectPath\n})\n```\n\n**路径说明**：\n- 使用与第4步相同的 `gitLogsPath` 和 `projectPath`（仅非master分支仓库）\n- `fileName`: 从Java文件路径中提取的类名\n- `filePath`: 相对于项目根目录的Java文件路径\n\n### 6. 检查是否有需要生成测试的内容\n\n在生成测试前，先检查是否有符合条件的变更：\n- 如果所有仓库都在master分支（第3步筛选结果为空），提示用户：\"所有仓库都在master分支，没有需要生成单元测试的内容。\"\n- 如果有非master分支但没有Java文件变更，提示用户：\"非master分支仓库没有Java文件变更，无需生成单元测试。\"\n- 只有存在非master分支且有Java文件变更的仓库，才继续生成测试\n\n读取git_logs目录中的差异文件，分析每个变更文件：\n\n**重要说明**：\n- 此步骤只处理第3步筛选出的非master分支仓库\n- 如果所有仓库都在master分支，在第3步就已经跳过，直接到第9步提示用户\n- 只分析有Java文件变更的非master分支仓库\n\n**识别测试范围**：\n- **新增方法**：完整覆盖所有分支和边界条件\n- **修改方法**：覆盖变更的逻辑分支\n- **删除方法**：移除对应的测试用例\n\n**关注点**：\n- 新增的方法签名（`public/private/protected` + 方法名）\n- 修改的if/else分支\n- 新增的API调用\n- 新增的配置项使用\n- 异常处理逻辑变更\n- 涉及到要mock的属性或者方法，如果是private声明的，需要删除掉修饰符\n\n根据被测试类的路径自动定位或创建测试类：\n\n**Java源文件路径转换规则**：\n- 源文件：`src/main/java/com/**/**/UserService.java`\n- 测试文件：`src/test/groovy/com/**/**/UserServiceTest.groovy`\n\n**转换步骤**：\n1. 将 `src/main/java` 替换为 `src/test/groovy`\n2. 将 `.java` 替换为 `Test.groovy`\n3. 类名添加 `Test` 后缀\n\n### 9. 生成测试代码\n\n**测试框架**：Spock + Groovy + Mockito\n\n**测试结构**：\n```groovy\npackage com.company.module.service\n\nimport spock.lang.Specification\nimport org.mockito.MockedStatic\nimport org.mockito.Mockito\n\nclass UserServiceTest extends Specification {\n    \n    private MockedStatic<StaticClass> staticMock\n    \n    def \"方法名 should 预期行为 when 测试条件\"() {\n        given: \"准备测试数据和mock\"\n        // 设置测试数据\n        // 配置mock行为\n        \n        when: \"执行被测试方法\"\n        // 调用方法\n        \n        then: \"验证结果\"\n        // 断言\n        \n        cleanup: \"清理资源\"\n        staticMock?.close()\n    }\n}\n```\n\n**测试覆盖场景**：\n1. **正常流程** - 方法正常执行返回预期结果\n2. **边界条件** - null值、空字符串、空集合等\n3. **异常情况** - API调用失败、数据库异常等\n4. **业务逻辑分支** - if/else各分支都要覆盖\n\n**参数化测试策略**：\n- 优先使用where表（当多个测试用例只有输入输出不同时）\n- 使用多个测试方法（当需要不同的mock配置或测试逻辑完全不同时）\n\n### 10. 验证测试，一定验证\n为每个生成的测试类执行验证流程：\n\n**验证步骤**（以central-so-service为例）：\n\n1. **进入服务maven父目录**：\n```javascript\n// 例如：central-customer-service\n// 需要cd到 central-customer-service 目录（服务maven父目录）\n```\n\n2. **编译测试模块**：\n```javascript\nexecutePwsh({\n  command: \"mvn clean install -DskipTests -pl central-customer-service -am\",\n  path: \"central-customer-service\"  // 服务maven父目录\n})\n```\n\n3. **运行单元测试**：\n```javascript\nexecutePwsh({\n  command: \"mvn test -Dtest=OrderShipServiceTest -pl central-customer-service\",\n  path: \"central-customer-service\"  //  服务maven父目录\n})\n```\n\n**路径规则**：\n- 如果服务路径是 `central-so/central-so-service`，则：\n  - path参数使用：`central-so`（父目录）\n  - -pl参数使用：`central-so-service`（模块名）\n- 如果服务路径是 `central-rma/central-rma-service`，则：\n  - path参数使用：`central-rma`（父目录）\n  - -pl参数使用：`central-rma-service`（模块名）\n\n**对每个测试类执行验证**：\n- 先编译模块（只需执行一次）\n- 再运行该测试类\n- 如果测试失败，报告错误信息\n- 如果测试通过，继续下一个\n\n生成和验证完成后，向用户发送：\n```\n单元测试生成并验证完成！\n\n已处理的服务和测试类：\n[列出每个服务及其测试类]\n\n验证结果：\n✓ [服务名]/[测试类名] - 测试通过\n✗ [服务名]/[测试类名] - 测试失败（错误信息）\n\n手动验证命令（如需重新测试）：\n1. cd [服务父目录]\n2. mvn clean install -DskipTests -pl [模块名] -am\n3. mvn test -Dtest=[测试类名] -pl [模块名]\n\n注意事项：\n- git_logs目录已保留，可查看代码变更详情\n- 测试代码已覆盖核心逻辑分支和边界条件\n- 如有测试失败，请review测试代码并根据实际业务调整\n```\n\n## 测试编写规则\n\n### Mock静态方法\n```groovy\nprivate MockedStatic<SSLClientUtil> sslClientUtilMockedStatic\n\ndef \"test case\"() {\n    given:\n    sslClientUtilMockedStatic = Mockito.mockStatic(SSLClientUtil.class)\n    sslClientUtilMockedStatic.when(() -> SSLClientUtil.doGet(Mockito.anyString(), Mockito.anyInt())).thenReturn(mockResponse)\n    \n    when:\n    // 执行测试\n    \n    then:\n    // 验证\n    \n    cleanup:\n    sslClientUtilMockedStatic?.close()\n}\n```\n\n### Mock实例方法\n```groovy\nservice.dao.update(_, _) >> 1\nservice.redisService.remove(_, _) >> null\n```\n\n### 验证方法调用次数\n```groovy\nthen:\n1 * service.dao.update(id, data)\n0 * service.dao.update(_, _)\n```\n\n### Where表参数化测试\n```groovy\ndef \"validate email should return correct result\"() {\n    when:\n    def result = service.validateEmail(email)\n    \n    then:\n    result == expected\n    \n    where:\n    email              | expected\n    \"test@test.com\"    | true\n    \"invalid\"          | false\n    null               | false\n    \"\"                 | false\n}\n```\n\n## 注意事项\n1. **两轮扫描优化**：\n   - 第一轮：快速获取所有仓库分支信息，筛选非master分支\n   - 第二轮：仅对非master分支仓库收集Java文件变更信息\n   - 如果所有仓库都在master分支，第二轮完全跳过\n2. **所有命令使用executePwsh工具执行**\n3. **git输出必须使用UTF-8编码**\n4. **只测试变更的方法和逻辑分支**\n5. **保留原有测试用例，不要删除**\n6. **一次性完成所有测试用例的编写**\n7. **不要运行测试验证结果**\n8. **Mock声明在类级别，cleanup中关闭**\n9. **git_logs目录会保留，方便用户review**\n10. **优化效果**：如果13个仓库中只有2个在非master分支，可减少约75%的git操作\n\n请开始执行任务。"
  }
}
