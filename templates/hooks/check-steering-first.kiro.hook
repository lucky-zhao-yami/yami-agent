{
  "enabled": true,
  "name": "优先匹配 Steering 规则",
  "description": "每次收到用户提问时，提醒 agent 按 cs-global-config.md 的意图分类表匹配问题，只加载匹配的规则文件。",
  "version": "2",
  "when": {
    "type": "promptSubmit"
  },
  "then": {
    "type": "askAgent",
    "prompt": "所有回复必须使用简体中文。按 cs-global-config.md 的意图分类表匹配用户问题，只读取匹配的 cs-*.md 规则文件，禁止全量加载所有规则文件。匹配不到时告知用户该问题暂未收录，建议联系对应负责人。"
  }
}
