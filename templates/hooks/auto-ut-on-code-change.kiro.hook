{
  "name": "代码变更后自动生成UT",
  "description": "在代码变更后自动触发，根据知识库中的UT规范生成单元测试",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": ["**/*.java"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "检测到 Java 文件变更，是否需要为变更的代码生成单元测试？\n\n**重要**：生成测试前，请先搜索知识库获取 UT 编写规范：\n```\n/knowledge search \"Spock Mock 测试规范\"\n```\n\n如果用户确认需要生成测试，请按以下步骤执行：\n\n## 1. 分析变更的代码\n- 识别新增/修改的方法\n- 确定需要 Mock 的依赖\n- 识别需要覆盖的分支和边界条件\n\n## 2. 参考知识库规范\n搜索并应用以下规范：\n- Mock 静态方法使用 `StaticMockHelper`\n- Mock HttpServletRequest 使用 `BaseData.mockRequest()`\n- 被 Mock 的字段不能是 private\n- 测试方法命名：`方法名 should 预期行为 when 测试条件`\n\n## 3. 生成测试代码\n- 测试文件路径：`src/main/java` → `src/test/groovy`，`.java` → `Test.groovy`\n- 使用 Spock + Groovy + Mockito\n- 真正调用被测方法，不要只模拟逻辑\n\n## 4. 验证测试\n```bash\nmvn test -Dtest=XxxTest -pl xxx-service\n```\n\n请询问用户是否需要生成测试。"
  }
}
